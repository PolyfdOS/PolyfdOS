name: Build polyfdOS

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  release:
    types: [ created ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          build-essential \
          nasm \
          grub-pc-bin \
          xorriso \
          genisoimage \
          qemu-system-x86
          
    - name: Display tool versions
      run: |
        echo "=== Tool Versions ==="
        gcc --version | head -n1
        nasm -version
        ld --version | head -n1
        genisoimage --version 2>&1 | head -n1
        echo "===================="
        
    - name: Clean previous builds
      run: make clean || true
        
    - name: Build polyfdOS kernel and ISO
      run: |
        echo "Building polyfdOS..."
        make
        echo "Build completed!"
        
    - name: Verify build artifacts exist
      run: |
        echo "=== Checking Build Artifacts ==="
        if [ ! -f kernel.elf ]; then
          echo "ERROR: kernel.elf not found!"
          exit 1
        fi
        if [ ! -f polyfdos.iso ]; then
          echo "ERROR: polyfdos.iso not found!"
          exit 1
        fi
        echo "✓ All artifacts found"
        echo "================================"
        
    - name: Display build artifacts info
      run: |
        echo "=== Build Artifacts ==="
        ls -lh kernel.elf polyfdos.iso
        echo ""
        echo "Kernel Binary:"
        file kernel.elf
        echo ""
        echo "ISO Image:"
        file polyfdos.iso
        echo "======================="
        
    - name: Test boot in QEMU (headless)
      run: |
        echo "=== Testing Boot Sequence ==="
        timeout 10s qemu-system-i386 \
          -cdrom polyfdos.iso \
          -m 32M \
          -serial stdio \
          -display none \
          -no-reboot || true
        echo "Boot test completed (timeout is expected)"
        
    - name: Create release directory
      run: |
        mkdir -p release
        cp polyfdos.iso release/
        cp kernel.elf release/
        
    - name: Generate build info
      run: |
        cat > release/BUILD_INFO.txt << EOF
        polyfdOS Build Information
        ==========================
        
        Build Date: $(date -u +"%Y-%m-%d %H:%M:%S UTC")
        Git Commit: ${{ github.sha }}
        Git Branch: ${{ github.ref_name }}
        Builder: GitHub Actions
        Workflow Run: ${{ github.run_number }}
        
        Kernel Binary:
        - File: kernel.elf
        - Size: $(stat -c%s kernel.elf) bytes
        - Type: $(file -b kernel.elf)
        
        Bootable ISO:
        - File: polyfdos.iso
        - Size: $(stat -c%s polyfdos.iso) bytes
        - Type: $(file -b polyfdos.iso)
        
        Build Environment:
        - OS: Ubuntu (GitHub Actions Runner)
        - GCC: $(gcc --version | head -n1)
        - NASM: $(nasm -version)
        
        Running Instructions:
        ---------------------
        QEMU:
          qemu-system-i386 -cdrom polyfdos.iso
        
        VirtualBox:
          1. Create new VM (Type: Other, Version: Other/32-bit)
          2. Attach polyfdos.iso as CD-ROM
          3. Boot from CD-ROM
        
        Real Hardware:
          sudo dd if=polyfdos.iso of=/dev/sdX bs=4M status=progress
          (WARNING: Replace /dev/sdX with your USB drive)
        
        Repository: ${{ github.server_url }}/${{ github.repository }}
        Organization: Daftyon
        License: MIT
        EOF
        
    - name: Generate checksums
      run: |
        cd release
        sha256sum polyfdos.iso kernel.elf > SHA256SUMS.txt
        md5sum polyfdos.iso kernel.elf > MD5SUMS.txt
        cat SHA256SUMS.txt
        
    - name: Upload ISO as artifact
      uses: actions/upload-artifact@v4
      with:
        name: polyfdos-iso
        path: release/polyfdos.iso
        retention-days: 90
        
    - name: Upload kernel as artifact
      uses: actions/upload-artifact@v4
      with:
        name: polyfdos-kernel
        path: release/kernel.elf
        retention-days: 90
        
    - name: Upload complete release package
      uses: actions/upload-artifact@v4
      with:
        name: polyfdos-complete-${{ github.sha }}
        path: release/*
        retention-days: 90
        
    - name: Create release summary
      run: |
        echo "### polyfdOS Build Summary 🇲🇦" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Status:** ✅ Build Successful" >> $GITHUB_STEP_SUMMARY
        echo "**Commit:** \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
        echo "**Branch:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Artifacts:**" >> $GITHUB_STEP_SUMMARY
        echo "- 📀 polyfdos.iso ($(stat -c%s release/polyfdos.iso | numfmt --to=iec-i --suffix=B))" >> $GITHUB_STEP_SUMMARY
        echo "- 🔧 kernel.elf ($(stat -c%s release/kernel.elf | numfmt --to=iec-i --suffix=B))" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Download:** Check the Artifacts section below" >> $GITHUB_STEP_SUMMARY
        
    - name: Upload to Release (on tag)
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: |
          release/polyfdos.iso
          release/kernel.elf
          release/BUILD_INFO.txt
          release/SHA256SUMS.txt
          release/MD5SUMS.txt
        body: |
          ## polyfdOS ${{ github.ref_name }} 🇲🇦
          
          **Moroccan x86 Operating System - Official Release**
          
          ### Download
          - `polyfdos.iso` - Bootable ISO image
          - `kernel.elf` - Kernel binary (ELF format)
          
          ### Quick Start
          ```bash
          # Run with QEMU
          qemu-system-i386 -cdrom polyfdos.iso
          ```
          
          ### Verify Download
          ```bash
          sha256sum -c SHA256SUMS.txt
          ```
          
          Built with ❤️ in Morocco by Daftyon
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  test-bochs:
    runs-on: ubuntu-latest
    needs: build
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Install dependencies with Bochs
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          build-essential \
          nasm \
          grub-pc-bin \
          genisoimage \
          bochs \
          bochs-x
          
    - name: Build polyfdOS
      run: make
      
    - name: Update bochsrc for headless mode
      run: |
        sed -i 's/display_library: sdl/display_library: term/g' bochsrc.txt
        sed -i 's/path=hydra.iso/path=polyfdos.iso/g' bochsrc.txt
        cat bochsrc.txt
        
    - name: Test with Bochs (headless)
      run: |
        echo "=== Testing with Bochs ==="
        timeout 10s bochs -f bochsrc.txt -q || true
        echo "Bochs test completed"
        
    - name: Upload Bochs logs
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: bochs-logs
        path: |
          bochslog.txt
          com1.out
        retention-days: 30